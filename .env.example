# Example environment variables for Vercel/GitHub

# Public (injected at build-time for the client bundle)
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=YOUR_THIRDWEB_CLIENT_ID
NEXT_PUBLIC_DROP_ERC20=0xYourDropERC20AddressOnSomniaTestnet
NEXT_PUBLIC_TOKEN_SYMBOL=STT

# Server-side only (set these in Vercel Project Settings → Environment Variables)
# Choose ONE auth method for Pinata:
PINATA_JWT=
# OR legacy key/secret:
PINATA_KEY=
PINATA_SECRET=
